import { Box, styled } from "@mui/material";

export const TypeUsersFormCreate = styled(Box)(({theme}) => ({
    display:'flex',
    flexDirection:'column',
    justifyContent:'center',
    alignItems : 'center',
    padding: theme.spacing(20)
}));